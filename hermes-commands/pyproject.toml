[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "hermes-commands"
version = "0.1.0"
description = "OpenCode session slash commands for Hermes Agent — /oc list, /oc show, /oc prompt."
requires-python = ">=3.11"
